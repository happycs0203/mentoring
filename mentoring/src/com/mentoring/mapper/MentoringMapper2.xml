<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MentoringMapper2">


<select id="dateInput" resultType="project">
select u_id, p_title, p_enroll 
from projectmento
</select>

<!-- 개설된 멘토링 목록 보기 -->
<select id="confirmRequestList" resultType="project">
select p_num, p_title, u_id, p_enroll 
from projectmento
</select>

</mapper>